from config import PERPLEXITY_API_KEY

def agent_response(prompt, context=""):
    # placeholder for LLM response or Perplexity API call
    # combine prompt + context
    return f"Simulated AI Response for: {prompt}\nContext: {context}"
